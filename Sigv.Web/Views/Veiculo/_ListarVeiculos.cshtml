@model IEnumerable<Sigv.Domain.Veiculo>

@{
    Layout = null;
}

@if (Model.Count() > 0)
{
    <table>

        <thead>
            <tr>
                <th class="responsive-screen-1">Veículo Id</th>
                <th class="responsive-screen-1">Marca</th>
                <th class="responsive-screen-2">Ano</th>
                <th class="responsive-screen-1">Placa</th>
                <th class="responsive-screen-3">Chassi</th>
                <th class="responsive-screen-2">Condição</th>
                <th class="responsive-screen-2">Status</th>
            </tr>
        </thead>

        <tbody>
            @foreach (var veiculo in Model)
            {
                <tr class="cursor-clicker" onclick="window.location.href = '/Veiculo?Id=@veiculo.VeiculoId'">
                    <td class="responsive-screen-1">@veiculo.VeiculoId.ToString("0000")</td>
                    <td class="responsive-screen-1">@veiculo.Marca @veiculo.Modelo</td>
                    <td class="responsive-screen-2">@($"{veiculo.AnoFabricacao}/{veiculo.AnoModelo}")</td>
                    <td class="responsive-screen-1">@veiculo.Placa</td>
                    <td class="responsive-screen-3">@veiculo.Chassi</td>
                    <td class="responsive-screen-2">@veiculo.Condicao.Nome</td>
                    <td class="responsive-screen-2">@veiculo.Status.Nome</td>
                </tr>
            }
        </tbody>


    </table>
}
else
{
    <span>Nenhum resgistro localizado</span>
}