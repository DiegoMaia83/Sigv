<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Sigv.Mobile.Laudo.Views.Laudos.PageAvarias"
             x:Name="bindingContextLaudo"
             Title="PageAvarias">
    <StackLayout Padding="20">
        <ListView x:Name="listViewAvarias" HasUnevenRows="True">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout Orientation="Horizontal">
                            <CheckBox IsChecked="{Binding IsChecked}" HorizontalOptions="Start" VerticalOptions="Center" CheckedChanged="CheckBoxAvaria_CheckedChanged" />
                            <Label x:Name="lbDescricao" Text="{Binding Descricao}" HorizontalOptions="Start" VerticalOptions="Center"/>
                            <Label x:Name="lbAvaria" Text="{Binding AvariaId}" />
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <StackLayout VerticalOptions="End">
            <Button Text="Voltar" Clicked="BtnGoToLaudo_Clicked"/>
        </StackLayout>

    </StackLayout>
</ContentPage>