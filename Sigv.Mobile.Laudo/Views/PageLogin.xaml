<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Sigv.Mobile.Laudo.Views.PageLogin"
             Title="PageLogin">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="200"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="200"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        

        <StackLayout Grid.Row="0" Grid.Column="0" Padding="30" BackgroundColor="AntiqueWhite">
            <Label Text="Logo"/>            
        </StackLayout>

        <StackLayout Grid.Row="1" Grid.Column="0" Padding="30" BackgroundColor="AliceBlue">
            <Label Text="Usuário" />
            <Entry x:Name="UsernameEntry" />
            <Label Text="Senha" />
            <Entry x:Name="PasswordEntry" IsPassword="True" />
            <Button x:Name="LoginButton" Text="Login" />
        </StackLayout>

        <StackLayout Grid.Row="2" Grid.Column="0" Padding="30" BackgroundColor="AntiqueWhite">
            <Label Text="Logo"/>
        </StackLayout>

    </Grid>
    
</ContentPage>